from __future__ import annotations

from {{ context_name }}.{{ aggregate_module }}.application.{{ aggregate_name }}_service import (
    {{ aggregate_name | capitalize }}Service,
)


def {{ aggregate_name }}_service(
    service: {{ aggregate_name | capitalize }}Service,
) -> {{ aggregate_name | capitalize }}Service:
    """Expose the wired {{ aggregate_name }} service to entrypoints.

    ARCH-017 forbids any context module from importing bootstrap/ directly, so
    this provider does not import it either -- main.py, outside any context,
    builds the container and passes its wired services in.
    """
    return service
